const Portfolio = ({state, quizAgain}) => {
    return (
        <>
        <div style={{display: state ? 'block' : 'none'}}>
            <h1>portfolio!!</h1>
        </div>
        </>
    );
}

export default Portfolio;